# PKScreener Dependencies - Python 3.12
# Last updated: 2024-09 (compatible with v0.46.20250914.779)

# Core technical analysis libraries
advanced_ta==0.1.8
TA-Lib>=0.6.0  # Install separately if needed. See README.

# Progress and CLI
alive-progress==1.6.2
click>=8.1.7
Halo>=0.0.31
rich>=13.9.2
tabulate>=0.9.0

# Web scraping and parsing
bs4>=0.0.2
lxml==4.9.4  # 5.0.0 or above causes problems

# Git and version control
gitpython>=3.1.43

# Google Sheets integration
gspread>=5.12.4
gspread_pandas>=3.3.0

# Job scheduling and processing
joblib>=1.4.2

# Machine Learning and Data Science
keras==3.6.0  # Compatible with tensorflow 2.16.1
numpy==1.26.4  # Required for tensorflow 2.16.1 and other packages
pandas>=2.2.3
scikit-learn>=1.5.2
scipy<=1.12.0  # cdflib removed from >=1.13.0 which breaks the build
tensorflow==2.16.1  # https://github.com/tensorflow/tensorflow/issues/62075

# Data formats and file handling
openpyxl>=3.1.5
Pyarrow>=17.0.0
xlsxwriter>=3.2.0

# This is required because https://github.com/erocarrera/pefile/issues/420
# and https://github.com/pyinstaller/pyinstaller/issues/8762
# and https://github.com/pyinstaller/pyinstaller/issues/7269
pefile>=2023.2.7,<2024.8.26

# Image processing
Pillow>=9.5.0

# PKScreener ecosystem packages
PKDevTools>=0.13.20251210.306
PKNSETools>=0.1.20250122.139
PKBrokers>=0.1.20251210.33

# Browser automation
pyppeteer>=2.0.0

# Timezone handling
pytz>=2024.2

# Telegram bot
python-telegram-bot>=13.4,<20.0

# HTTP requests and caching
requests_cache>=1.2.1
requests_ratelimiter>=0.7.0
pyrate_limiter>=2.10.0
urllib3>=1.26.20

# Backtesting and trading
vectorbt>=0.26.2

# Note: pandas-ta-classic is excluded due to numpy>=2.0.0 requirement
# which conflicts with tensorflow 2.16.1. TA-Lib should be used instead.
# If you need pandas-ta functionality without TA-Lib, you'll need to
# choose between tensorflow and pandas-ta-classic.
fonttools>=4.61.0 # not directly required, pinned by Snyk to avoid a vulnerability
tornado>=6.5.3 # not directly required, pinned by Snyk to avoid a vulnerability
