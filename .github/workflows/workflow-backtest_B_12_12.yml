# Project             :   Screenipy
# Author              :   Pranjal Joshi
# Created             :   24/01/2022
# Description         :   Workflow for downloading pickled stock data to cloud [Use this when yfinance causing an issue]

name: B. B_12_12 Backtest for  15 Minute Price & Volume breakout (B_12_12)
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Backtest for  15 Minute Price & Volume breakout (B_12_12)'
        required: false
        default: 'Backtest for  15 Minute Price & Volume breakout (B_12_12)'
  schedule:
  - cron: '22 10 * * 1-5'

jobs:

  B_12_12:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
      with:
        ref: Backtest-Reports

    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: 3.9

    - name: Restore Dependencies from Cache
      uses: actions/cache@v2
      with:
        path: ~\AppData\Local\pip\Cache
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install TA-Lib
      run: |
        python -m pip install --upgrade pip
        cd .github/dependencies/
        echo %cd%
        pip install TA_Lib-0.4.19-cp39-cp39-win_amd64.whl

    - name: Install Python Dependencies
      run: |
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
        pip3 install ta-lib
        pip install .

    - name: Run backtest
      shell: cmd
      continue-on-error: true
      run: |
        rmdir /s /q Backtest-Reports
        mkdir Backtest-Reports
        python pkscreener/pkscreenercli.py -a Y -e -p -o B:30:12:12

    - name: Push backtest and summary reports
      run: |
        copy "PKScreener_*.html" "Backtest-Reports"
        git config user.name github-actions
        git config user.email github-actions@github.com
        git config remote.origin.fetch "+refs/heads/*:refs/remotes/origin/*"
        git remote update
        git fetch
        git checkout -b Backtest-Reports origin/Backtest-Reports
        git pull
        git add Backtest-Reports/PKScreener_B_12_12_backtest_result_StockSorted.html --force
        git add Backtest-Reports/PKScreener_B_12_12_Summary_StockSorted.html --force
        git commit -m "GitHub-Action-Workflow-Backtest-Reports-(B_12_12)"
        git push -v -u origin +Backtest-Reports
